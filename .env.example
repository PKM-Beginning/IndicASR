# Base URL of the FastAPI backend, no trailing slash.
# Leave empty to run the frontend in demo/mock mode with no backend.
# Local development example: VITE_API_URL=http://localhost:8000
VITE_API_URL=
